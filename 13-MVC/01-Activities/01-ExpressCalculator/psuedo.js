/** 
 * we want to build an app that resembles the functionality of a calculator
 * responds to get request
 * returns the output of operand and two values
 * example 1 + 2 = 3 when running the add operand
 */

 // create an express app
 require express
 define port
 create app

 // watch for the request
 // we are watching for a get request
 app.get('route',(req, res)=>{
     // get params 
     // perform operand on the variables
     // add param 1 + param 2
     // sub param 1 + param 2
     // divide param 1 + param 2
     // multiply param 1 + param 2

     //return response to the client (aka browser/postman/etc)
 });

 // initiate the server to listen on the port we defined
 app.listen(PORT, {
     //log out that server has started
 });